

<div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                  <div class="x_title">
                    <h2>{{ p1h2 }}</h2>
                    <div class="col-md-2 pull-right"><a href="/admin/{{ locale }}/users/create/" class="btn btn-success btn-xs"><i class="fa fa-plus"></i> {% trans %}Create a new user{% endtrans %}</a></div>
                    <div class="clearfix"></div>
                  </div>
                  <div class="x_content">
                    <p class="text-muted font-13 m-b-30">
                      The Buttons extension for DataTables provides a common set of options, API methods and styling to display buttons on a page that will interact with a DataTable. The core library provides the based framework upon which plug-ins can built.
                    </p>
                    <table id="datatable-buttons" class="table table-striped table-bordered">
                      <thead>
                        <tr>
                          <th></th>
                          <th>{% trans %}Username{% endtrans %}</th>
                          <th>{% trans %}Name{% endtrans %}</th>
                          <th>{% trans %}Email{% endtrans %}</th>
                          <th>{% trans %}Color<{% endtrans %}/th>
                          <th>{% trans %}Langue{% endtrans %}</th>
                          <th>{% trans %}Rights and Access{% endtrans %}</th>
                          <th>{% trans %}Active{% endtrans %}?</th>
                          <th>{% trans %}Action{% endtrans %}</th>
                        </tr>
                      </thead>
                      <tbody>
                        {% for user in users %}
                        <tr>
                          <td>
                            {% if ( user.imagePath ) %}
                              <img src="{{ asset('uploads/pic/profile/' ~ user.imagePath) | imagine_filter('my_thumb') }}" alt="" class="img-circle profile_img" width="50" height="50" style="margin-top:0px;margin-left:0px;width:50px;height:50px;">
                            {% else %}
                              <img src="{{ url('homepage') }}build/images/profilpic.png" alt="..." class="img-circle profile_img" width="50" height="50" style="margin-top:0px;margin-left:0px;width:50px;height:50px;">
                            {% endif %}
                          </td>
                          <td>{{ user.username }}</td>
                          <td>{{ user.lastName }} {{ user.firstName }}</td>
                          <td>{{ user.email }}</td>
                          <td>{{ user.userColor }}</td>
                          <td>{{ user.locale }}</td>
                          <td>{{ user.role }}</td>
                          <td>{% if user.isActive == '1' %}
                            <span class="label label-success">{% trans %}Active{% endtrans %}</span>
                            {% else %}
                            <span class="label label-danger">{% trans %}Not active{% endtrans %}</span>
                            {% endif %}</td>
                          <td><a href="edit/{{ user.id }}" class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i> {% trans %}Edit{% endtrans %}</a> <a href="edit/{{ user.id }}" class="btn btn-danger btn-xs"><i class="fa  fa-trash"></i> {% trans %}Delete{% endtrans %}</a> <a href="edit/{{ user.id }}" class="btn btn-warning btn-xs"><i class="fa fa-lock"></i> {% trans %}Desactivate{% endtrans %}</a></td>
                        </tr>
                        {% endfor %}
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>