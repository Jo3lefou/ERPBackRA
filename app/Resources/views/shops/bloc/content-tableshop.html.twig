

<div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                  <div class="x_title">
                    <h2>{{ p1h2 }}</h2>
                    <div class="col-md-2 pull-right"><a href="/admin/{{ locale }}/shops/create/" class="btn btn-success btn-xs"><i class="fa fa-plus"></i> {% trans %}Create a new shop{% endtrans %}</a></div>
                    <div class="clearfix"></div>
                  </div>
                  <div class="x_content">
                    <p class="text-muted font-13 m-b-30">
                      The Buttons extension for DataTables provides a common set of options, API methods and styling to display buttons on a page that will interact with a DataTable. The core library provides the based framework upon which plug-ins can built.
                    </p>
                    <table id="datatable-buttons" class="table table-striped table-bordered">
                      <thead>
                        <tr>
                          <th>{% trans %}Email{% endtrans %}</th>
                          <th>{% trans %}Address<{% endtrans %}/th>
                          <th>{% trans %}Email{% endtrans %}</th>
                          <th>{% trans %}Language{% endtrans %}</th>
                          <th>{% trans %}Phone{% endtrans %}</th>
                          <th>{% trans %}Active{% endtrans %}</th>
                          <th>{% trans %}Action{% endtrans %}</th>
                        </tr>
                      </thead>
                      <tbody>
                        {% for shop in shops %}
                        <tr>
                          <td>{{ shop.extention }} - {{ shop.name }}</td>
                          <td>{{ shop.address }} {{ shop.zipCode }} {{ shop.city }}, {{ shop.country }}</td>
                          <td>{{ shop.email }}</td>
                          <td>{% if shop.locale == 'fr' %}
                              {% trans %}Français{% endtrans %}
                            {% else %}
                              {% trans %}Anglais{% endtrans %}
                            {% endif %}</td>
                          <td>{{ shop.phone }}</td>
                          <td>
                            {% if shop.isActive == '1' %}
                            <span class="label label-success">{% trans %}Active{% endtrans %}</span>
                            {% else %}
                            <span class="label label-danger">{% trans %}Not active{% endtrans %}</span>
                            {% endif %}
                          </td>
                          <td><a href="edit/{{ shop.id }}" class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i> {% trans %}Edit{% endtrans %}</a> <a href="edit/{{ shop.id }}" class="btn btn-danger btn-xs"><i class="fa  fa-trash"></i> {% trans %}Delete{% endtrans %}</a> <a href="edit/{{ shop.id }}" class="btn btn-warning btn-xs"><i class="fa fa-lock"></i> {% trans %}Desactivate{% endtrans %}</a></td>
                        </tr>
                        {% endfor %}
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>