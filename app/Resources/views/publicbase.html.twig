<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="Formulaire - Form | Rime Arodaky">
    <meta name="author" content="Rime Arodaky">
    <link rel="icon" href="http://www.rime-arodaky.com/wp-content/uploads/2016/10/favicon.png">

    <title>{% block title %}{% endblock %}</title>

    <!-- Bootstrap core CSS -->
    <link href="{{ url('homepage') }}vendors/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Cormorant+Garamond:300,300i,400,400i,500,500i,600,600i,700,700i|Poppins:300,400,500,600,700" rel="stylesheet">
    <link href="{{ url('homepage') }}publicform/css/jquery-ui.min.css" rel="stylesheet">
	  <link href="{{ url('homepage') }}publicform/css/jquery-ui.structure.min.css" rel="stylesheet">
	  <link href="{{ url('homepage') }}publicform/css/jquery-ui.theme.min.css" rel="stylesheet">
    <link href="{{ url('homepage') }}publicform/css/style.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="container">
     
      <div class="header clearfix line-bottom">
        <h3 class="text-center"><a href=""><img src="{{ url('homepage') }}publicform/img/logo-noir.png" alt="Rime Arodaky." width="100" height="30"/></a></h3>
      </div>

      <div class="row marketing">
		  	<div class="col-lg-3">
		  		
		  	</div>
			<div class="col-lg-6">
			{% if error is not empty %}
        <div class="alert alert-danger" role="alert"><strong>Uuuuh...</strong>{{ error }}</div>
        <div class="text-center">
          <a class="btn btn-default black" href="http://www.rime-arodaky.com" style="display:inline-block">Go back to our website...</a>
        </div>
      {% else %}
        {% block body %}{% endblock %}
      {% endif %}
			</div>
     		<div class="col-lg-3">
		  		
		  	</div>
      </div>


	
	

      <footer class="footer">
        <p>2018 &copy; RIME ARODAKY – Tous droits réservés</p>
      </footer>

    </div> <!-- /container -->

   {% block footer %}{% endblock %}

  </body>
</html>
